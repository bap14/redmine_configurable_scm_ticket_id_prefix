redmine_configurable_scm_ticket_id_prefix
=========================================

Redmine module to allow admin ability to adjust the scm ticket reference prefix from "#" to any other string (or strings)
